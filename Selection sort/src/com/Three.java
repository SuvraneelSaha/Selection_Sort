package com;

public class Three {
    // STEPS :
    // 1] find the largest element from the array
    // it will run <N - i  times or <= N - i - 1 no. of times
    // 2 ] swap the element with the element which is lying in the correct position
    // 3 ] already the right side elements are at the correct positions
    // so it will be ignored in future steps

}
